%h1 Confirmation

- if items.any?
  .confirmations
    .d-flex.justify-content-between.mb-4
      .btn-group
        = button_to 'Clear all', confirmation_path, method: :delete,
                    form: { data: { turbo_confirm: 'Are you sure?' } }, class: 'btn btn-danger'
      .btn-group
        - sorted_by_name = sort == 'name'
        = link_to 'Sort by group', confirmation_path(sort: 'group'), class: "btn #{sorted_by_name ? 'btn-outline-primary' : 'btn-primary'}"
        = link_to 'Sort by name', confirmation_path(sort: 'name'), class: "btn #{sorted_by_name ? 'btn-primary' : 'btn-outline-primary'}"

    %ul.list-group
      - items.each do |item|
        = render 'item', item: item

- else
  .alert.alert-secondary
    First add items in the #{link_to('shopping list', shopping_list_path)}.
