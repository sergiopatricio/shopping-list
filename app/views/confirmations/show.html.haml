%h1 Confirmation

- if items.any?
  .confirmations
    .d-flex.justify-content-between.mb-4
      .btn-group
        = button_to 'Reset', confirmation_path, method: :delete,
                    form: { data: { turbo_confirm: 'Reset confirmations?' } }, class: 'btn btn-warning'
      .btn-group
        - sorted_by_name = sort == 'name'
        = link_to confirmation_path(sort: 'group'), class: "btn #{sorted_by_name ? 'btn-outline-primary' : 'btn-primary'}" do
          %span.icon-custom.icon-sort-group Sort by group
        = link_to confirmation_path(sort: 'name'), class: "btn #{sorted_by_name ? 'btn-primary' : 'btn-outline-primary'}" do
          %span.icon-custom.icon-sort-name Sort by name

    %ul.list-group
      - items.each do |item|
        = render 'item', item: item

- else
  .alert.alert-secondary
    First add items in the #{link_to('shopping list', shopping_list_path)}.
