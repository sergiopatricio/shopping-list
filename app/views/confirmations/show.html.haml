%h1 Confirmation

- if @items.any?
  %p= link_to 'Clear all confirmations', confirmation_path, method: :delete, class: 'btn btn-danger', data: { confirm: 'Unconfirm all items?' }
  %p
    - sorted_by_name = @sort == 'name'
    = link_to 'Sort by group', confirmation_path(sort: 'group'), class: "btn #{sorted_by_name ? 'btn-outline-info' : 'btn-info'}"
    = link_to 'Sort by name', confirmation_path(sort: 'name'), class: "btn #{sorted_by_name ? 'btn-info' : 'btn-outline-info'}"

  %ul.list-group.confirmations
    - @items.each do |item|
      = render 'item', item: item

- else
  .alert.alert-secondary
    First add items in the #{link_to('shopping list', shopping_list_path)}.
