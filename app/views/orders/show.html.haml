%h1 Order confirmation

- if @items.any?
  %p= link_to 'Clear confirmations', order_path, method: :delete, class: 'btn btn-danger'

  %ul.list-group.order-confirmation
    - @items.each do |item|
      = render 'item', item: item

- else
  %p No items
