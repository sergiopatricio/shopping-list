%h1 Order confirmation

- if @items.any?
  %p= link_to 'Clear all confirmations', order_path, method: :delete, class: 'btn btn-danger', data: { confirm: 'Unconfirm all items?' }
  %p
    - sorted_by_name = @sort == 'name'
    = link_to 'Sort by group', order_path(sort: 'group'), class: "btn #{sorted_by_name ? 'btn-outline-info' : 'btn-info'}"
    = link_to 'Sort by name', order_path(sort: 'name'), class: "btn #{sorted_by_name ? 'btn-info' : 'btn-outline-info'}"

  %ul.list-group.order-confirmation
    - @items.each do |item|
      = render 'item', item: item

- else
  .alert.alert-secondary
    First add items in the #{link_to('shopping list', shopping_list_path)}.
